<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TLUProjectExpo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">28sp</item> <!-- Or your desired size -->
        <item name="android:textStyle">bold</item>
        <!-- You can also set the color here if needed, but your toolbar already has app:titleTextColor -->
        <!-- <item name="android:textColor">@android:color/black</item> -->
    </style>
    <style name="Theme.TLUProjectExpo" parent="Base.Theme.TLUProjectExpo" />
</resources>